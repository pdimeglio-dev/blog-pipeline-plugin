# Copy to .env and fill in. The .env file is gitignored.
#
# OPENAI_API_KEY — used by the Writer skill to call OpenAI Images API (DALL-E 3)
# for blog cover generation. Get one at https://platform.openai.com/api-keys.
OPENAI_API_KEY=
